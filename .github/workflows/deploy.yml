name: Deploy to ECS

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # Debug environment variables
    - name: Debug environment variables
      run: |
       echo "ECR_REPOSITORY = ${{ vars.USERNAME}}"
        


 
