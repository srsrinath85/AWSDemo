name: Deploy to ECS

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # Debug environment variables
    - name: Debug environment variables
      run: |
        echo "AWS_REGION = ${{ secrets.AWS_ACCESS_KEY_ID }}"
        echo "AWS_ACCOUNT_ID = ${{ secrets.AWS_SECRET_ACCESS_KEY}}"
        echo "ECR_REPOSITORY = ${{ secrets.AWS_REGION}}"
        


 
